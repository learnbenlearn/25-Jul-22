window.onload = function() {
    let divElement = document.querySelector('#divContainer');
    let buttons = document.querySelectorAll('button');
    //let innerButtonsText = buttons.map(x => x.innerHTML);
    console.log(buttons);
}